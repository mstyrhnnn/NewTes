export enum OrderApprovalStatusEnum {
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    REJECTED = 'rejected',
}

export enum OrderStatusEnum {
    PENDING = 'pending',
    WAITING = 'waiting',
    CONFIRMED = 'confirmed',
    ON_GOING = 'on_going',
    DONE = 'done',
}